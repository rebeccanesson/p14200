#include <stdio.h> 
#include <cs50.h> 
#include <math.h> 

int main(void) { 
    
    // Ask the user for a side length
    
    // Store the answer in a variable
    
    // Ask the user for the other side length
    
    // Store the answer in a variable
    
    // Calculate the hypotenuse length using sqrt
    
    // Tell the user the length of the hypotenuse 

}
